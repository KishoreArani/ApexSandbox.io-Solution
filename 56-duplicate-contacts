public Boolean duplicateContacts(Contact c1, Contact c2) {
    //code here
    if(c1.Email != Null && c2.Email!= Null && c1.Email == c2.Email || 
    c1.phone != null && c2.phone != null && c1.Phone == c2.Phone){
        return true;
    }

    return False;
}