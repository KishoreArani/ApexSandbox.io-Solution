public Boolean sameParent(Account acc, Contact con, Opportunity opp) {
    //code here
     if(acc.Id != Null && con.AccountId != Null && opp.AccountId != null && con.AccountId == opp.AccountId &&
     con.AccountId == acc.Id && opp.AccountId == acc.Id){
        return true;
    }

    return false;
}